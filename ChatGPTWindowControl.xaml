<UserControl x:Class="ChatGPTExtension.GptToolWindowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vsshell="clr-namespace:Microsoft.VisualStudio.Shell;assembly=Microsoft.VisualStudio.Shell.15.0" xmlns:controls="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
             Background="{DynamicResource {x:Static vsshell:VsBrushes.WindowKey}}"
             Foreground="{DynamicResource {x:Static vsshell:VsBrushes.WindowTextKey}}"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             Name="MyToolWindow" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- The button to trigger the code transfer from VS.NET to GPT -->
        <Button Grid.Row="0" Grid.Column="0" Content="VS.NET to GPT ➡️" Click="OnSendCodeButtonClick" Tag="" Height="30" FontFamily="Segoe UI Emoji"/>

        <!-- Fix code from VS.NET to GPT -->
        <Button Grid.Row="0" Grid.Column="1" Content="Fix code in GPT ➡️" Click="OnSendCodeButtonClick" Tag="Fix code below:" Height="30" FontFamily="Segoe UI Emoji"/>

        <!-- Improve code from VS.NET to GPT -->
        <Button Grid.Row="0" Grid.Column="2" Content="Improve code in GPT ➡️" Click="OnSendCodeButtonClick" Tag="Improve code below:" Height="30" FontFamily="Segoe UI Emoji"/>

        <!-- The button to trigger the code transfer from GPT to VS.NET -->
        <Button Grid.Row="1" Grid.ColumnSpan="3" Content="⬅️ GPT to VS.NET" Click="OnReceiveCodeButtonClick" Height="30" FontFamily="Segoe UI Emoji"/>

        <!-- The WebView2 control with Chat GPT -->
        <controls:WebView2 Grid.Row="2" Grid.ColumnSpan="3" Name="webView" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
    </Grid>


</UserControl>
