# ChatGPTExtension

Chat GPT extension will load Chat GPT web and integrate with Visual Studio 2022.

Works with FREE and Plus Chat GPT web version. No Open AI tokens are required.

**Features**

- Send selected code from VS.NET to GPT
- Send selected code from GPT to VS.NET
- When GPT "Copy code" button is clicked code is sent automatically to VS.NET
- Buttons to automate fixing code and improving code
- Other code operations like commenting code, adding exception handling, creating unit tests and several others
- Configurable custom actions to apply in code by GPT that can be added, removed, changed or reordered as needed

**Demo**

![GPT Extension Demo](https://i.ibb.co/4PB1q5s/Chat-GPT-Extension-Demo.gif)

**Install**

1. Close VS.NET
2. Download extension and install
3. Open VS.NET
4. **Go to menu View -> Other Windows and select Chat GPT Extension**
5. Log into Chat GPT

**Uninstall**

1. In VS.NET go to extensions -> manage extensions, click installed, select and remove the Chat GPT Extension
2. Close VS.NET to apply the changes
3. Delete the directory %appdata%\\..\Local\ChatGPTExtension

**Usage**

"VS.NET to GPT" button will copy selected code from VS.NET to GPT prompt area keeping any data already there.

"Fix code in GPT" button will copy selected code from VS.NET to GPT prompt area asking to fix it.

"Improve code in GPT" button will copy selected code from VS.NET to GPT prompt area asking to improve it.

"GPT to VS.NET" button will copy selected text from GPT and send to VS.NET.

"Enable Copy Code" switch when enabled will automatically copy the code to VS.NET when Copy code button in GPT is clicked.

"Arrow" this will open submenu with other code operations GPT can do for you.

"Reload Chat GPT..." submenu will attempt reloading GPT in the internal browser.

"Configure extension..." submenu will open the screen to configure the code actions operations.

**Whats new**

2.0 - Drag and drop support for reordering custom actions. Fixes detecting changes to be saved.
      Maximum of 20 custom actions can be added now.

1.7 - Custom actions can now be added/removed/edited/reordered in a grid. Now current GPT prompt will be cleared for custom actions to run. Note: due to migration existing customized actions from version 1.6 will be lost. Sorry about that!

1.6 - Configurable custom actions to apply in code by GPT

1.5 - Now sending detected language name when available to GPT for better response accuracy.

1.4 - Fix issues with end and home keys inside GPT prompt textbox. Added other actions to process code in submenu.

1.3 - Initial release.

**Known Issues**

There are some known random issues logging off from Chat GPT. If you can't logoff, try clicking F12 then trying again or close VS.NET and delete directory %appdata%\\..\Local\ChatGPTExtension

**Licensing**

The extension is free to use but cannot be cloned, modified or sold in any way.